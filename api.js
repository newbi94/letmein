import { QueryFunction } from "@tanstack/react-query";

/* export const INsApi = () => {
  trending : ({ pageParam }) => 
    fetch(`${BASE_URL}/trending/movie/week?api_key=${API_KEY}&language=en-US&page=${ pageParam ?? 1}`)
    .then((res) => res.json()), 
}; */
